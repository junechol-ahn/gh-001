name: 02 - Workflow Events

on: 
  push:
  pull_request:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
  

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger
        run: |
          echo this is triggered by ${{github.event_name}}
          echo $(date)
      
